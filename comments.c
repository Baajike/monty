
#include "monty.h"

/**
 * comments - Does nothing (a placeholder for the comments opcode).
 * @stack: Pointer to the head of the stack.
 * @line_number: Line number in the file.
 */
void comments(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
